# הצעת מחיר — מערכת סולארית לבית | כהן מערכות אנרגיה

גרסת הקהל הרחב של אתר הצעת המחיר (מבנה אפליקציה, GSAP). זהה ויזואלית לגרסת ערד — עם קופי שפונה לבעלי בתים פרטיים.

- קובץ יחיד: `index.html` — ללא build, GSAP מוצמד מ-CDN.
- דומיין: `offer.maslolim.cohen-energy.com` (קובץ `CNAME`, GitHub Pages).
- עריכת תוכן: כל המספרים והטקסטים הדינמיים באובייקט `DATA` בראש ה-`<script>`.

## DNS

רשומת CNAME אצל רשם הדומיין: `offer.maslolim` → `barbossaaa.github.io`.
